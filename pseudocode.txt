public class Main
{
    
    public void play() {
        Board user = new Board();
        EnemyBoard enemy = new EnemyBoard();
        enemy.remaining();
        boolean finished = false;
        boolean cheat = true;
        int move = 0;
        char key;
        while (!finished) {
            key = c.getChar();
            while (key != "ENTER" && !enemy.visited[enemy.cursorX][enemy.cursorY]) {
                if (key is wasd) {
                    enemy.moveCursor(key);
                } else if (key is cheat && cheat) {
                    enemy.cheat();
                }
                key = c.getChar();
            }
            if (enemy.hit()) {
                enemy.sank();
                enemy.remaining();
                if (shipSunk == {true, true, true, true, true}) finished = true;
            }
            if (!finished) {
                enemyTurn();
            }
            
        }
    }
    
    public void enemyTurn() {
        
    }
    
	public static void main(String[] args) {
		System.out.println("Hello World");
	}
}
